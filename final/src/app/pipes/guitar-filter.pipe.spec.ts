import { GuitarFilterPipe } from './guitar-filter.pipe';

describe('GuitarFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GuitarFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
